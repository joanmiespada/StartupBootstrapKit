{
	"private": true,
	"devDependencies": {
		"lerna": "^2.11.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"pretest": "lerna exec --parallel -- yarn pretest",
		"bootstrap": "lerna bootstrap",
		"test": "lerna exec --parallel -- yarn test",
		"test-easy": "lerna exec --parallel -- yarn test-easy",
		"nodemon":"lerna exec --parallel -- yarn nodemon",
		"start":"lerna exec --parallel -- yarn start",
		"build-dev":"lerna exec --parallel -- yarn build-dev",
		"build-prod":"lerna exec --parallel -- yarn build-prod",
		"serve-prod":"lerna exec --parallel -- yarn serve-prod"
	}
}
